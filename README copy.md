# Result Analysis Portal — Frontend

## Run

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173

The frontend talks to the backend at `http://localhost:5000/api`.

Admin login: `admin` / `admin123`

Start the API first:

```bash
cd backend
npm run dev
```
